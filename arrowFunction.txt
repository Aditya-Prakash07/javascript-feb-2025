1️⃣ Understanding Arrow Functions (=>)

Arrow functions are a shorter way to write functions in JavaScript.

✅ Traditional Function Syntax:

function add(a, b) {
    return a + b;
}
console.log(add(2, 3)); // 5
✅ Same Function as an Arrow Function:

const add = (a, b) => a + b;
console.log(add(2, 3)); // 5
Key Features of Arrow Functions:

Shorter syntax
No need for return when there’s only one expression.
Implicit return for single-line expressions.
2️⃣ Understanding forEach()

forEach() is a method used to loop over an array. It executes a function for each item in the array.

✅ Example: Using forEach() to Print an Array

let fruits = ["Apple", "Banana", "Mango"];

fruits.forEach(function(fruit) {
    console.log(fruit);
});
Output:

Apple
Banana
Mango
✅ Same Example Using an Arrow Function

fruits.forEach(fruit => console.log(fruit));
3️⃣ Understanding Template Literals (``)

Template literals allow us to embed variables directly inside strings using ${}.

✅ Example Without Template Literals (+ Concatenation)

let name = "Aditya";
console.log("Hello, " + name + "!"); // Old way
✅ Same Example With Template Literals (Modern)

let name = "Aditya";
console.log(`Hello, ${name}!`); // Modern way (cleaner)
🚀 Applying Everything Together
Now let’s break down your original code:

authors.forEach(author => {
    console.log(`${author.author} wrote: ${author.books.join(", ")}`);
});
Step-by-Step Explanation:
authors.forEach(author => {...})
Loops through each object in the authors array.
The current author object is passed to the arrow function (author).
Inside the Function:
console.log(`${author.author} wrote: ${author.books.join(", ")}`);
${author.author}: Accesses the author's name.
${author.books.join(", ")}: Joins the books array into a string.
✅ Example Array:

const authors = [
    { author: "George Orwell", books: ["1984", "Animal Farm"] },
    { author: "J.R.R. Tolkien", books: ["The Hobbit", "The Lord of the Rings"] }
];
✅ Output of the Loop:

George Orwell wrote: 1984, Animal Farm
J.R.R. Tolkien wrote: The Hobbit, The Lord of the Rings
🔹 Summary

Concept	Explanation
Arrow Functions (=>)	Shorter function syntax, automatic return for single-line expressions.
forEach()	Loops over arrays, executes a function for each element.
Template Literals ( )	Allows embedding variables inside strings using ${}.
🛠 Practice Exercise
Try writing a similar loop using normal functions instead of an arrow function:

authors.forEach(function(author) {
    console.log(author.author + " wrote: " + author.books.join(", "));
});
This will give the same output as the arrow function version.

